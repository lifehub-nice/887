# CLAUDE.md, 887studio.fr

> Produit du studio 887. **Le protocole, la doctrine et les motifs vivent dans la factory**,
> pas ici. Ce fichier ne fait que pointer, puis dire ce qui est propre à ce dépôt.

## À lire avant d'agir, dans cet ordre

1. `~/Dev/factory/BOUSSOLE.md` : le pourquoi et les principes non négociables.
2. `~/Dev/factory/CLAUDE.md` : le protocole de session. Ouvrir, faire, fermer.
3. `~/Dev/factory/docs/PATTERNS.md` : les motifs et pièges déjà payés une fois.
   **Se lit avant de concevoir, pas après.**
3bis. `~/Dev/factory/doctrine/founder-model.md` : **comment Vincent tranche.** À lire pour
   challenger comme il challengerait. Interdit d'en déduire une décision à sa place.
4. `~/Dev/factory/docs/REGIMES.md` : ce que le régime ci-dessous impose.
5. `~/Dev/factory/tracks/studio.md` : la prochaine action, et rien d'autre.

## Ce qui s'applique partout dans le studio

- **Interdiction du tiret cadratin** « — » et du demi-cadratin « – », dans tout texte produit.
- **MANUEL** : nom, domaine, prix, périmètre, secrets, paiement réel, console externe, GO de lancement.
- **Fermer la session** : mettre à jour la fiche de voie et `~/Dev/factory/ETAT.md`, logger dans `~/Dev/factory/DECISIONS.md`, committer.
- **On nomme les fichiers qu'on commite.** `git add <chemin>`, jamais `git add -A` : plusieurs sessions travaillent en parallèle (motif 39).

## Régime : ACTIF

Ce dépôt est de l'outillage, pas un produit. Détail dans `~/Dev/factory/docs/REGIMES.md`.

- **Aucun secret, jamais.** Seulement des `.env.example`.
- Une modification ne se justifie que par un besoin réellement rencontré deux fois.


## Ce produit

> La doctrine dit comment travailler. Ces fichiers disent **sur quoi**. Une session qui
> connaît le studio et pas le produit ne sert à rien.

- `README.md`

<!-- 887:couches-1-et-2 -->

